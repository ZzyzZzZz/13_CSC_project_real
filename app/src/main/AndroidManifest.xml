<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhu.a13cscproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.13CSCProject">
        <activity android:name=".SendData"
            android:screenOrientation="portrait"/>
        <activity android:name=".OrderConfirm_v2"
            android:screenOrientation="portrait"/>
        <activity android:name=".UpdateActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".FoodSpecifications"
            android:screenOrientation="portrait"/>
        <activity android:name=".FoodChoice_v2"
            android:screenOrientation="portrait"/>
        <!-- update to my comment, this is to carry variable over, so I am kind of extending .FoodChoice_v2 class -->
        <activity android:name=".IdentityCheck"
            android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>